{{yield (hash
  header=(component "base/table-header" updateSort=updateSort)
  body=(component "base/table-body" records=records)

  thead=(component "base/table-thead" notifySortObservers=notifySortObservers)
)}}
