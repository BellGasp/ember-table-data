{{#if filtersRows.length}}
    {{yield (hash
      filterButton=(component 'core/filter/filter-button' click=filter)
      clearButton=(component 'core/filter/filter-button' click=clearFilters)
      )
    }}
{{/if}}
