{{#if (has-block)}}
  {{yield properties filterRowObject.property }}
{{else}}
  {{#power-select
      selected=filterRowObject.property
      options=properties
      onChange=(action (mut filterRowObject.property))
      tagName="div"
      searchField="label"
      class="property-selector"
      as |filter|
  }}
    {{get filter "label"}}
  {{/power-select}}
{{/if}}
