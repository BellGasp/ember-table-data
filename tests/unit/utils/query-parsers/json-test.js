import queryParsersJson from 'dummy/utils/query-parsers/json';
import { module, test } from 'qunit';

module('Unit | Utility | query-parsers/json', function(hooks) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = queryParsersJson();
    assert.ok(result);
  });
});
